import { Check, LockKeyhole, MapPinned, ShieldCheck } from "lucide-react";
import { fixedSurveyGroups, fixedSurveyStrategies } from "../../data/fixedSurvey";
import { Badge } from "../ui/Badge";
import { Card } from "../ui/Card";

export function FixedSurveyGuide() {
  return <div className="space-y-6"><div><div className="flex items-center gap-2 text-indigo-600 dark:text-indigo-400"><ShieldCheck className="h-5 w-5" /><span className="text-sm font-semibold">STEP 1. 서베이 고정 가이드</span></div><h1 className="mt-2 text-2xl font-bold text-zinc-950 dark:text-white sm:text-3xl">시험 전, 이 선택 조합으로 답변 범위를 고정합니다.</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-zinc-600 dark:text-zinc-300">OPIc Background Survey의 일반적인 진행 순서를 참고해 만든 고정 선택본입니다. 실제 시험 화면의 표현은 운영 시점에 따라 달라질 수 있지만, 이 앱에서는 아래 조합을 바꾸지 않고 훈련합니다.</p></div><Card className="border-indigo-200 bg-indigo-50 p-4 dark:border-indigo-900 dark:bg-indigo-950"><div className="flex flex-wrap items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-md bg-indigo-600 text-white"><LockKeyhole className="h-4 w-4" /></span><div><p className="text-sm font-semibold text-indigo-900 dark:text-indigo-100">OOM 고정 서베이 조합</p><p className="text-xs leading-5 text-indigo-700 dark:text-indigo-300">시험 당일에도 이 구성을 기준으로 선택해, 말할 장면을 4개로 관리합니다.</p></div><Badge className="ml-auto" tone="indigo">변경 없음</Badge></div></Card><section className="grid items-start gap-5 xl:grid-cols-[minmax(0,1fr)_320px]"><div className="space-y-4">{fixedSurveyGroups.map((group) => <Card className="overflow-hidden" key={group.id}><div className="flex items-start gap-4 border-b border-zinc-100 bg-zinc-50 px-5 py-4 dark:border-zinc-800 dark:bg-zinc-950"><span className="grid h-8 w-8 shrink-0 place-items-center rounded bg-indigo-600 text-xs font-bold text-white">{group.order}</span><div><p className="text-base font-bold text-zinc-900 dark:text-white">{group.title}</p><p className="mt-1 text-xs leading-5 text-zinc-500 dark:text-zinc-400">{group.description}</p></div></div><div className="divide-y divide-zinc-100 dark:divide-zinc-800">{group.answers.map((answer) => <div className="px-5 py-4" key={answer.prompt}><p className="text-sm font-medium text-zinc-700 dark:text-zinc-200">{answer.prompt}</p><div className="mt-3 flex items-start gap-3"><span className={`grid h-5 w-5 shrink-0 place-items-center rounded ${answer.kind === "multiple" ? "bg-indigo-600 text-white" : "border-2 border-indigo-600"}`}>{answer.kind === "multiple" ? <Check className="h-3.5 w-3.5" /> : <span className="h-2 w-2 rounded-full bg-indigo-600" />}</span><p className="text-sm leading-6 text-zinc-900 dark:text-zinc-100">{answer.answer}</p></div></div>)}</div></Card>)}</div><aside className="space-y-5 xl:sticky xl:top-24"><Card className="p-5"><div className="flex items-center gap-2"><MapPinned className="h-5 w-5 text-emerald-500" /><h2 className="text-base font-bold text-zinc-900 dark:text-white">선택 → 답변 장면</h2></div><div className="mt-4 space-y-4">{fixedSurveyStrategies.map((strategy) => <div key={strategy.group}><div className="flex items-center justify-between gap-2"><p className="text-sm font-semibold text-zinc-800 dark:text-zinc-100">{strategy.group}</p><Badge tone="emerald">고정</Badge></div><p className="mt-1 text-xs leading-5 text-zinc-500 dark:text-zinc-400">{strategy.items}</p><p className="mt-2 text-xs leading-5 text-zinc-700 dark:text-zinc-300">{strategy.summary}</p></div>)}</div></Card><Card className="border-amber-200 bg-amber-50 p-5 dark:border-amber-900 dark:bg-amber-950"><p className="text-sm font-bold text-amber-900 dark:text-amber-100">선택 기준</p><p className="mt-2 text-sm leading-6 text-amber-800 dark:text-amber-200">더 많은 항목을 고르는 것이 목적이 아닙니다. 같은 경험을 여러 질문에 자연스럽게 바꿔 말할 수 있으면 충분합니다.</p></Card></aside></section></div>;
}
